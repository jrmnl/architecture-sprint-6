apiVersion: v1
kind: Service
metadata:
  name: {{ include "scaletestapp-chart.fullname" . }}
  labels:
    {{- include "scaletestapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scaletestapp-chart.selectorLabels" . | nindent 4 }}
