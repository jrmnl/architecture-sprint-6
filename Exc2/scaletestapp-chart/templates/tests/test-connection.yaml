apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaletestapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "scaletestapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaletestapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
